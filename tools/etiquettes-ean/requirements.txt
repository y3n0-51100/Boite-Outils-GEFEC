# Dépendances de l'outil « Étiquettes EAN — Douchette virtuelle »
# Moteur d'extraction PDF principal (robuste, s'embarque proprement dans le .exe)
pymupdf>=1.24
# Repli si PyMuPDF est indisponible
pypdf>=4.0
pyautogui>=0.9.54
# Interface web embarquée (WebView2 sous Windows)
pywebview>=5.0
# Construction du .exe (uniquement pour générer l'exécutable) :
pyinstaller>=6.0
